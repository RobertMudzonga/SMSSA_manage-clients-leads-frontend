// Toggle to disable client-side uploads. When false, clients cannot upload documents
// and project managers should mark checklist items as received manually.
export const CLIENT_UPLOADS_ENABLED = false;

// Future: per-visa overrides may be added here.
export const MANUAL_CHECKLIST_ONLY = true;
